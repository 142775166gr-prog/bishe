<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.zhjypt.mapper.ChapterContentMapper">

    <!-- 根据章节ID获取内容列表 -->
    <select id="getContentsByChapterId" resultType="com.example.zhjypt.pojo.ChapterContent">
        SELECT * FROM chapter_content 
        WHERE chapter_id = #{chapterId} AND del = 0 
        ORDER BY sort_order ASC, create_time ASC
    </select>

    <!-- 获取章节内容数量 -->
    <select id="getContentCountByChapterId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM chapter_content 
        WHERE chapter_id = #{chapterId} AND del = 0
    </select>

    <!-- 更新内容排序 -->
    <update id="updateContentSort">
        UPDATE chapter_content 
        SET sort_order = #{sortOrder}, update_time = NOW() 
        WHERE content_id = #{contentId}
    </update>

</mapper>